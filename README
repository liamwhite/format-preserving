Format-preserving encryption: generate random permutations of potentially large sets using cryptography.

Do not use this when cryptographic security is desired; use a validated library. I am not a cryptographer, and weak security in my implementation does not matter for my uses.
